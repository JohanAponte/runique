package com.example.core.domain.util

/**
 * A marker interface representing an error type.
 *
 * This interface is used as a base for defining custom error types
 * in the application domain. By implementing this interface, you can
 * create specific error classes that can be used in conjunction with
 * the `Result` sealed interface to represent error states.
 */
interface Error